/**
 * 1) Create a new array that doubles each number in the original array
 * 2) Create a new array that only holds the even numbers inside the original array
 * 3) Calculate the sum of all the numbers inside an array
 * 4) Function that returns true or false based on if every element of the array is larger than 18
 * */
