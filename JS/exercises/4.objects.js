// log all the complete names of all the authors in the this array
const authorsByBooks = {
    'Catcher in the rye': 'Jerome Salinger',
    'Of mice and men': 'John Steinbeck',
};

//Create a new object that holds no null values

//Create a new object that has no keys starting with _ ( these will be secret data )




























// Object.entries(authorsByBooks).forEach((entry) => {
//     const [book, author] = entry;
//     console.log(author + ' - ' + book);
// });
